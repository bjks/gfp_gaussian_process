class Parameter_set{
/*  Notation in Athos thesis:
    ---------------------------
    mean_lambda;     = \bar \lambda
    gamma_lambda;    = \gamma_\lambda
    var_lambda;      = \sigma_\lambda^2

    mean_q;          = \bar q
    gamma_q;         = \gamma_q
    var_q;           = \sigma_q^2

    beta;            = \beta

    var_x;           = \sigma_x^2
    var_g;           = \sigma_g^2

    var_dx;          = \sigma_{dx}^2
    var_dg;          = \sigma_{dg}^2
*/
public:
    double mean_lambda;
    double gamma_lambda;
    double var_lambda;

    double mean_q;
    double gamma_q;
    double var_q;
    
    double beta;

    double var_x;
    double var_g;

    double var_dx;
    double var_dg;

};
