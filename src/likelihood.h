
double total_likelihood(MOMAdata &cell){
    double likelihood = 0;
    if (cell.is_root()){
        /* code */
        ;
    }
    else if (cell.is_leaf()){
        /* code */
        ;
    }
    else{
        ;
    }
    
    for(long t=0; t<cell.time.size(); ++t){
        ;
    }

    return likelihood ;
}
